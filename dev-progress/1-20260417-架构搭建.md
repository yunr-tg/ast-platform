# 架构搭建

## 本次进展

- 完成 Maven 多模块父工程搭建
- 完成 `ast-platform-common`、`ast-platform-domain`、`ast-platform-api-contract`、`ast-platform-infra`、`task-worker-sdk` 模块骨架
- 完成 `task-gateway` 与 `task-scheduler` 启动工程骨架
- 按 `AGENTS.md` 要求在 `task-scheduler` 内隔离 `pump`、`dispatcher`、`retry`、`callback`、`notify`、`compensator` 包结构
- 完成任务提交、Worker 注册、Worker 心跳、Worker 回调等基础接口骨架
- 完成状态机基础模型、Redis Key 约定和 Lua 占位脚本
- 完成最小化测试并通过 `mvn test`

## 当前状态

- 当前已完成 P0 第一阶段工程骨架搭建
- 持久化、RocketMQ、Redis 调度、外盒、补偿等真实实现尚未落地

## 下一步建议

- 优先落 `task-gateway` 的任务落库与入队外盒
- 然后补 `task-scheduler` 的 Pump、Dispatcher 与 Worker 运行态存储
