# 网关功能开发

## 本次进展

- 完成网关提交主链路编排，支持任务提交、幂等命中和响应返回
- 完成基于 `requestId` 与 `tenantId + taskType + bizKey` 的重复提交识别
- 完成任务事实记录与入队外盒记录模型设计
- 完成网关内部仓储抽象与内存版实现，便于后续替换为 MySQL
- 完成消息发布抽象与日志型发布器，便于后续替换为 RocketMQ
- 完成外盒立即投递与定时重发表骨架
- 扩展提交响应，返回任务状态、幂等命中标记和外盒状态
- 补充网关接口测试，覆盖首次提交、重复 `requestId`、重复 `bizKey`
- 完成整仓 `mvn test` 验证

## 当前状态

- `task-gateway` 已具备可运行的 P0 提交骨架
- 当前持久化与消息发送仍为内存/模拟实现，尚未接入真实 MySQL 与 RocketMQ

## 下一步建议

- 将网关仓储替换为 MySQL 事实表与入队外盒表
- 引入 RocketMQ Producer 并将外盒投递改为真实发送
- 增加状态更新条件校验与版本控制
- 增加外盒失败重试次数、下一次重试时间和审计字段
